Add git repos to this folder, here is an example for fugitive

    git submodule add http://github.com/tpope/vim-fugitive.git bundle/fugitive
    git add .
    git commit -m "Install a bundle as a submodule."
